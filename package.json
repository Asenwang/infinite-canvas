{
    "name": "infinite-canvas-root",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "cd web && bun run dev",
        "build": "cd web && bun run build",
        "start": "cd web && bun run start"
    },
    "dependencies": {
        "next": "16.2.3"
    }
}
